---
description: 项目导航与代码索引使用规则。适用于需要定位模块、页面入口、Service/Factory/Registry、启动注册链或跨模块实现映射的任务；优先使用代码索引作为导航入口，再回到真实代码核实。
alwaysApply: true
enabled: true
---

# 项目导航规则

## 何时优先读取代码索引

- 当任务涉及以下任一场景时，先阅读 `docs/本地知识库/项目结构/代码索引.md`：
  - 需要定位页面、模块、目录归属
  - 需要查页面入口到实现的映射关系
  - 需要查 `Service` / `Factory` / `Registry` 的落点
  - 需要查启动链、`ModuleSetup`、`FrameworkServiceBridge.register(...)`
  - 需要理解模块职责、依赖分层、运行时装配关系
  - 需要在大范围仓库搜索前先缩小查找范围
- 当任务涉及查找组件需求文档与代码的映射关系时，先阅读 `docs/component-map.md`：
  - 需要通过需求文档找到对应代码
  - 需要了解某个页面的需求文档和代码实现位置
  - 需要查找某个组件的需求描述和实现映射
- 如果任务只是修改一个已知文件、已知函数或已明确路径的小范围改动，不必机械先读整份代码索引。

## 使用方式

- 代码索引的用途是 **导航、缩小范围、建立查找路径**，不是替代真实代码阅读。
- 组件映射文档 `docs/component-map.md` 的用途是 **快速查找需求文档与代码的映射关系**，特别适合需要通过需求文档找到对应代码的场景。
- 推荐顺序：
  1. 先读 `docs/本地知识库/项目结构/代码索引.md`
  2. 根据索引锁定候选模块、目录、入口文件
  3. 再回读对应源码、构建脚本、注册链文件做最终确认
- 当任务涉及组件需求文档与代码的映射时，推荐顺序：
  1. 先读 `docs/component-map.md`
  2. 根据映射关系找到对应的需求文档和代码文件
  3. 再回读对应源码做最终确认
- 当任务涉及真实依赖关系、注册顺序、页面装配链路时，必须以源码、构建脚本和运行时注册代码为准。

## 与其他规则的关系

- 涉及默认页面分层、通用 View / ViewModel / UseCase / Repository 职责时，遵循 `@.codebuddy/rules/通用架构规范/RULE.mdc`。
- 涉及 `PageWidget`、`DataRepo`、`StructComposePage`、多 tab、吸顶、悬浮等 Struct 页面能力时，遵循 `@.codebuddy/rules/Struct结构规范/RULE.mdc`。
- 涉及 Compose 组件、UI 规范、无障碍和主题约束时，遵循 `@.codebuddy/rules/UI规范/RULE.mdc`。

## 维护原则

- 代码索引文档是项目级知识入口，应保持路径稳定：`docs/本地知识库/项目结构/代码索引.md`。
- 组件映射文档 `docs/component-map.md` 是需求文档与代码映射的入口，应保持路径稳定，并随新增组件/页面及时更新。
- 当发现索引与真实代码不一致时，以真实代码为准；如果本次任务已经核实了新的模块映射或路径，应顺手更新索引文档。
- 当发现 `docs/component-map.md` 中的映射关系与真实代码不一致时，应顺手更新组件映射文档。
- 不要把零散模块知识塞进多份规则里；优先沉淀到代码索引或组件映射文档，再由本规则负责引导查阅。

## 禁止事项

- 不要把代码索引当成唯一事实来源。
- 不要在没有核对源码的情况下，仅根据索引文档判断“实现已存在”或“链路正确”。
- 不要在每个局部任务里都全文扫描索引文档；只有在需要全局定位、跨模块查找或理解架构映射时优先使用。
